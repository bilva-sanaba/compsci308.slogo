package parser.tokenNodes;

import model.Command;
import model.Token;

public class CommandNode extends TokenNode {

	public CommandNode(TokenNode parentNode, Command token) {
		super(parentNode, token);
		// TODO Auto-generated constructor stub
	}

}
